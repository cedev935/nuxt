// TODO: import plugins
// this.$nuxt.hook('')
// this.$nuxt.context ({ req, res, params, query, route })

export default async function (context) {
	// for each plugin
	// await plugin(contex, inject)
}
